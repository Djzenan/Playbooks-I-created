- name: Installs packages
  hosts: all 
  user: root
  tasks:
  - name: ensure a list of packages installed
    yum:
      name: "{{ packages }}"
    vars:
      packages:
      - telnet
      - unzip
      - lftp
      - elinks
      - wget

